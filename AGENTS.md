# AGENTS.md

LLM 코딩 실수를 줄이기 위한 행동 지침입니다. 프로젝트별 지침과 함께 병합해서 사용합니다.

**절충점:** 이 지침은 속도보다 신중함을 우선합니다. 사소한 작업에는 상황에 맞게 판단합니다.

## 1. 코딩 전에 생각하기

**가정하지 않습니다. 혼란을 숨기지 않습니다. 선택지와 절충점을 드러냅니다.**

구현 전에:

- 가정을 명확히 말합니다. 확실하지 않으면 묻습니다.
- 해석이 여러 개라면 조용히 하나를 고르지 말고 제시합니다.
- 더 단순한 접근이 있다면 말합니다. 필요할 때는 근거를 들어 반대 의견을 냅니다.
- 불명확한 부분이 있으면 멈추고 무엇이 혼란스러운지 말한 뒤 묻습니다.

## 2. 단순함 우선

**문제를 해결하는 최소 코드만 작성합니다. 추측으로 기능을 늘리지 않습니다.**

- 요청받지 않은 기능을 추가하지 않습니다.
- 한 번만 쓰는 코드에 추상화를 만들지 않습니다.
- 요구되지 않은 유연성이나 설정 가능성을 추가하지 않습니다.
- 실제로 불가능한 상황을 위해 오류 처리를 늘리지 않습니다.
- 200줄로 쓴 코드가 50줄로 가능하다면 줄입니다.

스스로 묻습니다. "시니어 엔지니어가 이걸 과하게 복잡하다고 볼까?" 그렇다면 단순화합니다.

## 3. 외과적으로 변경하기

**반드시 필요한 부분만 건드립니다. 내가 만든 부산물만 정리합니다.**

기존 코드를 수정할 때:

- 주변 코드, 주석, 포맷을 겸사겸사 개선하지 않습니다.
- 망가지지 않은 코드를 리팩터링하지 않습니다.
- 내가 다르게 쓰고 싶더라도 기존 스타일을 따릅니다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 삭제하지 않습니다.

내 변경 때문에 고아 코드가 생겼을 때:

- 내 변경으로 불필요해진 import, 변수, 함수는 제거합니다.
- 원래부터 있던 죽은 코드는 요청받지 않는 한 제거하지 않습니다.

검사 기준: 변경된 모든 줄은 사용자의 요청과 직접 연결되어야 합니다.

## 4. 목표 중심 실행

**성공 기준을 정의하고, 검증될 때까지 반복합니다.**

작업을 검증 가능한 목표로 바꿉니다.

- "검증 추가" -> "잘못된 입력 테스트를 작성하고 통과시킨다"
- "버그 수정" -> "버그를 재현하는 테스트를 작성하고 통과시킨다"
- "X 리팩터링" -> "리팩터링 전후 테스트가 통과하는지 확인한다"

여러 단계 작업이라면 짧은 계획을 먼저 말합니다.

```text
1. [단계] -> 검증: [확인 방법]
2. [단계] -> 검증: [확인 방법]
3. [단계] -> 검증: [확인 방법]
```

강한 성공 기준은 에이전트가 독립적으로 반복하며 완료하게 해 줍니다. "작동하게 만들기" 같은 약한 기준은 계속된 확인 질문을 필요로 합니다.

## 5. 단계 전환 확인

새 개발 단계에 진입하기 전에 `docs/worklog/HANDOFF.md`를 확인합니다. HANDOFF에 blocking backlog가 지정돼 있으면 이를 처리하거나 명시적으로 해제하기 전까지 다음 단계로 진행하지 않습니다.

## 6. 프로젝트 공통 기준

이 절은 모든 코딩 에이전트가 공통으로 따릅니다.

- 작업 시작 시 `docs/README.md`에서 현재 기준과 문서 우선순위를
  확인합니다.
- 새 개발 단계 전에는 `docs/worklog/HANDOFF.md`를 전체 읽고,
  blocking backlog를 먼저 처리하거나 명시적으로 해제합니다.
- 현재 동작을 변경하기 전 제품 요구사항, 자동화 경계, 상태 머신,
  아키텍처와 테스트 전략을 확인합니다.
- 코드와 문서가 충돌하면 임의로 해석하지 말고 코드·테스트·실측
  근거를 대조해 불일치를 보고합니다.
- 사이트 동작은 실측 근거와 fixture 없이 추측해 구현하지 않습니다.
- `docs/specs/automation-boundary.md`의 안전 경계와 durable claim,
  민감정보 비저장 원칙을 우회하지 않습니다.
- 과거 spec과 worklog는 이력으로 보존하고, 현재 기준처럼 사용하지
  않습니다.
- 코드·문서 변경 후 `npm run check`와 `git diff --check`를 실행합니다.
  문서 링크와 spec 인덱스 검사는 `npm run check`에 포함돼 있습니다.
- 커밋 제목은 Conventional Commits 형식을 사용합니다. 호환 기능은
  `feat:`, 호환 수정은 `fix:`, 파괴적 변경은 `type!:`로 표시합니다.
- `AGENTS.md`가 모든 AI의 공통 기준입니다. `CLAUDE.md`와
  `.claude/skills/`에는 Claude 전용 사용법만 추가합니다.
- 도구별 지침이 이 파일과 충돌하면 작업을 중단하고 보고합니다.

## 7. 작업 유형별 산출물

작업을 시작할 때 유형을 정하고, 끝낼 때 그 행의 산출물이 있는지
확인합니다. 유형이 애매하면 더 무거운 쪽을 따릅니다.

| 유형 | 브랜치 | 산출물 |
|---|---|---|
| 기능·리팩터 (spec 있음) | `feat`/`refactor` | spec `10`~`40` + HANDOFF + 워크로그 |
| 단발 수정 | `fix` | HANDOFF + 워크로그 |
| 문서 | `docs` | 해당 문서 + 인덱스 + 워크로그 |
| 릴리스·도구 | `chore` | 워크로그 |

브랜치 접두사와 병합 기준은 `docs/development/branch-strategy.md`,
릴리스 절차는 `docs/development/release-process.md`를 따릅니다.

### 워크로그는 색인이지 보고서가 아닙니다

`docs/worklog/YYYY-MM-DD-NN-<주제>.md`에 **세션마다** 남깁니다. 목적은
"언제 무슨 작업을 했는가"의 시간축 기록입니다. git log는 변경 단위라
세션에서 무엇을 결정하고 무엇을 버렸는지 담지 못합니다.

담는 것과 담지 않는 것을 구분합니다.

| 담는다 | 담지 않는다 |
|---|---|
| 한 것 (3~5줄) | 구현 세부 → `30-implementation` |
| 내린 결정과 근거 | 검증 수치 → `40-verification` |
| 검토했다 버린 선택지 | 현재 상태 → `HANDOFF` |
| 남은 것·미해결 | |
| 산출물 링크 (PR·spec·이슈) | |

**같은 값을 두 곳에 적지 않습니다.** 수치나 목록은 원본 문서를 링크하고
워크로그는 참조만 남깁니다. 중복은 반드시 어긋납니다.

---

**이 지침이 작동하고 있다는 신호:** diff에 불필요한 변경이 줄고, 과설계로 인한 재작업이 줄며, 구현 후가 아니라 구현 전에 명확화 질문이 나옵니다.
