{
  "name": "catchtable-reserve-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && esbuild src/content/index.ts --bundle --format=iife --target=chrome120 --outfile=dist/content/index.js && node scripts/copy-static.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/adapter.test.mjs",
    "perf": "npm run build && node tests/performance.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.318",
    "esbuild": "^0.25.12",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3"
  }
}
