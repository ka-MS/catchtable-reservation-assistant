{
  "name": "catchtable-reserve-assistant",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && tsc -p tsconfig.json && esbuild src/content/index.ts --bundle --format=iife --target=chrome120 --outfile=dist/content/index.js && node scripts/copy-static.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check:dist": "node scripts/check-dist.mjs",
    "check:independence": "node scripts/check-independence.mjs",
    "check": "npm run typecheck && npm test && npm run check:dist && npm run check:independence"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.318",
    "esbuild": "^0.25.12",
    "fake-indexeddb": "^6.2.4",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3"
  }
}
