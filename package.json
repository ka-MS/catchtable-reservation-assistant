{
  "name": "catchtable-reserve-assistant",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && tsc -p tsconfig.json && esbuild src/content/index.ts --bundle --format=iife --target=chrome120 --outfile=dist/content/index.js && esbuild src/main-world/availability-probe.ts --bundle --format=iife --target=chrome120 --outfile=dist/main-world/availability-probe.js && node scripts/copy-static.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test --test-concurrency=8 tests/*.test.mjs",
    "check:version": "node scripts/check-version.mjs",
    "check:dist": "node scripts/check-dist.mjs",
    "check:independence": "node scripts/check-independence.mjs",
    "analyze:rt14": "npm run build && node scripts/analyze-empty-early-exit.mjs",
    "check": "npm run check:version && npm run typecheck && npm test && npm run check:dist && npm run check:independence"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.318",
    "esbuild": "^0.25.12",
    "fake-indexeddb": "^6.2.4",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3"
  }
}
