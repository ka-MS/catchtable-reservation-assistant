# 자동화 경계와 사용자 인계

## 자동화가 수행하는 일

- 같은 오리진 매장 URL의 서버 시간 측정
- 실측된 달력 셀 판독과 인접 날짜/목표 날짜 클릭
- 실측된 슬롯 칩 판독, 시간 정규화, 우선순위 선택
- dry-run 결과 기록
- dry-run이 아닐 때 선택된 슬롯의 단일 클릭

## 자동화가 수행하지 않는 일

- 로그인 정보 입력
- CAPTCHA 또는 대기열 우회
- Catchtable 비공개 API 호출
- 인원 선택 상태를 추측하는 DOM 판정
- 슬롯 클릭 후 테이블 타입·예약금·방문 목적·약관·결제·확정 조작
- 외부 결제 도메인 조작

## 사용자 인계 조건

다음 조건에서는 `HANDED_OFF`로 전환한다.

1. 슬롯을 실제로 클릭한 직후
2. 실행 전 페이지가 준비되지 않아 사용자의 조작이 필요한 경우
3. 실측된 달력·슬롯 상태 어느 것으로도 판정할 수 없는 화면인 경우

인계 시 자동화는 모든 타이머와 observer를 정리하고 다시 개입하지 않는다. 사용자가 새 실행을 시작해야만 새로운 RunContext가 만들어진다.

## 최종 동작 안전 규칙

- 슬롯 클릭은 예약 완료나 결제 승인이 아니다.
- 본 MVP에는 슬롯 외 버튼을 클릭하는 코드가 존재하지 않는다.
- `COMPLETED`는 향후 예약 완료를 실측한 뒤 사용할 예약 상태이며 MVP에서는 도달하지 않는다.
- 실사이트 검증은 dry-run이 기본이다. 실제 슬롯 클릭 검증은 사용자의 명시적 합의가 필요하다.

## 실패와 재시도

- 무기한 pause 상태는 없다.
- 네트워크 시계 측정은 제한된 표본 수와 재측정 횟수만 사용한다.
- 날짜 토글은 감시 종료 시각까지만 반복한다.
- DOM click 실패는 같은 슬롯을 무한 재클릭하지 않고, 논리 슬롯 키를 기록한 뒤 제한된 다음 토글로 진행한다.
- 종료 시각에 도달하면 반드시 `TIMED_OUT`으로 종료한다.
