# XHR 슬롯 응답 감시 — 분석·설계·구현 계획

**기준일:** 2026-07-12 (정찰 반영 개정)
**선행:** 시계 노이즈 해소(worklog 11). openDelta가 실런 간 비교 가능해짐.
**상태:** R1·R2·R3·R5 실측 완료(site-behavior §8.1, worklog 12). R4(격리 월드 가시성)만 구현 1단계에서 확정. **구현 게이트 열림.**

## 1. 문제 분석 (현행 감지 메커니즘의 구조적 한계)

현재 감지 경로: 인접 날짜 클릭 → 목표 날짜 클릭 → (SPA가 슬롯 XHR 발사) → 응답 도착 → 렌더 → DOM 스캔.

orchestrator의 `runToggleCycle` 구조상 스캔이 허용되는 창은:

- 목표 클릭 후 고정 20ms sleep + 날짜 선택 확인 폴링(최대 60ms) **이후부터**
- **다음 사이클의 인접 클릭 시각(목표−40ms)까지만**

토글 간격 150ms 기준 실제 스캔 창은 사이클당 수십 ms다. 반면 슬롯 XHR 왕복은 ~100-200ms라, 클릭 N의 응답은 대부분 클릭 N의 스캔 창이 닫힌 뒤(= 인접 날짜 표시 중인 블라인드 구간이거나 다음 사이클 대기 중)에 도착한다. 결과: **감지가 "오픈 통과 첫 클릭 + 1~3사이클"로 양자화**된다. 진짜 오픈런에서는 +200~450ms 감지가 구조적 바닥이다.

목표: 감지 시각 ≈ 응답 도착 + 렌더 지연. 오픈 직후 클릭의 응답(+RTT ~100-200ms)을 즉시 잡으면 이론상 +100~250ms → 클릭까지 포함해 현행 대비 1~2사이클(150~300ms) 단축.

## 2. 실측 결과 (2026-07-12, 이시즈에 — 상세는 site-behavior §8.1)

| # | 결과 | 설계 반영 |
|---|---|---|
| R1 | `POST ct-api.catchtable.co.kr/api/reservation/v1/dining/time-slots?shopRef=…` (별도 API 호스트). URL에 날짜 없음 — 날짜는 요청 헤더 `yymmdd` | 필터 = pathname suffix `/dining/time-slots`. **도착 신호는 날짜 불문** → 스캔이 목표 날짜 선택 상태를 반드시 검증 |
| R2 | 날짜 셀 클릭마다 1회 발사. **같은 날짜 재클릭은 재발사 없음** | 인접 토글 유지(현행 전략 검증됨) |
| R3 | 클릭→발사 **217~379ms** + 왕복 52~63ms + 렌더 56~182ms = **클릭→렌더 330~560ms**. 발사 시점에 기존 슬롯 DOM이 비워짐 | 스캔 버스트 상한 ~250ms. **클릭→발사 지연이 최대 병목** — §3.1 콰이어스 필요 |
| R4 | main 월드에서 리소스 항목 경로째 관측 확인. 격리 월드는 미확정 | 구현 1단계에서 content script 내 실측으로 확정 (실패 시 폴백 경로만 동작 — 무해) |
| R5 | 가용 여부 무관 항상 200 + `timeSlotMap`. 도착 신호 항상 발생 | 신호만으로 충분. 가용 판정은 DOM 스캔 담당 |

**실오픈런 검증 데이터(worklog 12, 7/25 22:00 오픈, run-e2c48fb6):** 목표 클릭이 −15/+260/+556/+843/+1152ms에 나갔는데 감지는 **+1303ms**. 감지된 렌더는 +1152 클릭이 아니라 **+843 클릭의 응답**(파이프라인 지연과 정확히 일치). 사이클 2~4의 응답 렌더는 후속 토글 클릭이 밟아 소실됐다. → 관측(도착 신호)만 추가해서는 부족하고, **비행 중 요청을 밟지 않는 콰이어스가 핵심**이다.

## 3. 설계

### 방식 비교

| 방식 | 바디 | 침습성 | 평가 |
|---|---|---|---|
| **A. PerformanceObserver 리소스 타이밍** | 없음(도착 시각·URL만) | 없음 — 격리 월드에서 표준 API 구독 | **권장** |
| B. MAIN 월드 fetch/XHR 패치 | 있음 | 높음 — 페이지 주입, 사이트 갱신·CSP에 취약 | 보류 |
| C. chrome.webRequest (background) | 없음 | 낮음, 단 background→content 메시지 왕복 지연 추가 | A 실패 시 폴백 |
| D. chrome.debugger | 있음 | 디버깅 배너·과도한 권한 | 제외 |

A를 권장하는 근거: 감지에 필요한 것은 "응답이 도착했다 → 지금 스캔하라"는 **신호**뿐이다. 슬롯 클릭은 어차피 DOM 렌더를 기다려야 하므로, 바디를 파싱해 슬롯을 먼저 알아내도 이득이 렌더 지연(R3)만큼으로 제한된다. 신호만 쓰면 자동화 경계도 불변(관측만 추가, 클릭 대상·한계 변화 없음).

### 3.1 아키텍처 (정찰 반영 개정)

```
SlotRefreshWatchPort {
  start(onArrival: (arrivalAt: number) => void): void;  // 필터: pathname endsWith "/dining/time-slots"
  stop(): void;
}
```

- 어댑터 계층에 PerformanceObserver 구현(`src/content/adapter/slot-refresh-watch.ts`). 필터 미스면 콜백이 안 울릴 뿐 — 실패 모드가 무해.
- **오케스트레이터 통합 — 두 축:**
  1. **콰이어스(quiesce, 핵심)**: 목표 클릭 후 해당 클릭의 time-slots **도착 신호까지 다음 토글을 보류**한다(상한: 클릭+~700ms 타임아웃 → 신호 유실 시 현행 그리드로 복귀). 실오픈런에서 감지를 +1303ms로 밀어낸 원인이 "후속 토글이 비행 중 응답의 렌더를 밟는 것"이었으므로, 관측 추가보다 이것이 먼저다.
  2. **도착 신호 → 스캔 버스트**: 신호 수신 시 목표 날짜 선택 상태 확인 후 10ms 간격 버스트(상한 ~250ms — R3 렌더 지연 56~182ms + 여유). 블라인드 구간(인접 날짜 표시 중) 신호는 목표 재클릭 직후로 이연.
  3. **폴백**: 신호가 전혀 안 오는 환경(격리 월드 불가·URL 변경)에서는 현행 사이클-말 스캔 그대로 — 성능 회귀 없음.
- 계측: `xhrArrivalServerAtMs`·`arrivalToDetectMs`·`clickToFireMs`를 이벤트 data에 추가 — 개선 폭과 ct-api 게이트 시점(오픈 직후 첫 슬롯 포함 응답이 몇 ms에 오는지)을 실런에서 직접 검증.
- **기대 효과**: 오픈 통과 첫 클릭(+0) → 발사 +~280 → 응답 +~340 → 렌더 +~420 → 감지 ≈ **+430ms** (현행 실측 +1303ms 대비 ~870ms 단축).
- **2단계 후보(별도 스펙)**: 클릭→발사 지연(~250ms)을 역이용해 목표 클릭을 openAt−250ms에 배치하면 요청이 오픈 직후 서버에 도달 → 이론 감지 +150~250ms. 오픈 전 처리되어 빈 응답이 올 위험이 있어 콰이어스+계측으로 게이트 시점 실측 후 판단.

### 성능·안전 불변식

- Observer 콜백은 필터 매치 시에만 실행 — 핫패스 오버헤드 무시 가능. 버스트 스캔은 상한이 있어 CPU 폭주 없음.
- 자동화 경계 스펙 불변. 관측 계층 추가일 뿐 클릭 정책 변화 없음.
- R2 결과에 따라 인접 토글 폐기가 가능해지면(같은 날짜 재클릭으로 재요청 유발 시) 블라인드 구간 자체가 사라진다 — 별도 후속 스펙으로 분리.

## 4. 구현 계획 (TDD, 단계별 커밋)

1. ~~정찰(R1·R2·R3·R5)~~ **완료** (site-behavior §8.1, worklog 12). R4만 잔여 → 2단계에서 확장 content script에 임시 trace로 확정.
2. `slot-refresh-watch.ts` 어댑터 + 단위 테스트(fake PerformanceObserver로 필터·start/stop·콜백 검증). 실런 1회로 격리 월드 가시성(R4) 확정 — 불가 판정 시 방식 C(webRequest) 재평가로 회귀.
3. 오케스트레이터 콰이어스 + 도착 스캔 버스트 + 폴백 — 기존 fake-clock 하네스 확장(도착 신호 주입 → 감지 시각 단축 단언, 신호 없음 → 현행 그리드 경로 회귀 단언, 신호 유실 타임아웃 단언).
4. 계측 필드(`xhrArrivalServerAtMs`·`arrivalToDetectMs`·`clickToFireMs`) + trace-view 표시.
5. 실런 검증: 연습 실런 + 실오픈런에서 감지 시각 비교. 성공 기준: 실오픈 감지 ≤ +500ms(현행 +1303), 폴백 경로 회귀 없음.

## 5. 리스크

- **신호 누락**(사이트가 URL 패턴 변경): 폴백 스캔이 그대로 살아 있어 현행 수준 유지 — 성능 회귀 없음.
- **렌더 전 버스트 낭비**: 상한으로 제한, R3 실측으로 튜닝.
- **전송 방식 변경**(fetch→WebSocket 등): 방식 A 무력화되나 폴백으로 동작 유지, 그때 방식 C 재평가.
