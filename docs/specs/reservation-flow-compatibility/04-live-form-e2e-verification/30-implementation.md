# 04 실제 예약 폼 E2E 실행

상태: 완료

1. 최신 `dist`를 빌드하고 unpacked extension을 새로고침했다.
2. Side Panel 예약 작업을 `dryRun=false`로 실행했다.
3. Chrome DevTools로 실제 URL과 예약 정보 접근성 트리를 확인했다.
4. extension origin에서 `activeRun`과 IndexedDB `runs`/`events`를 조회했다.
5. 폼 도착 후 추가 클릭 없이 종료했다.
