# 2026-07-11 단조 서버 시계

## 수행

- 서버 epoch를 `performance.now()`에 앵커링했다.
- 대기, 종료, 토글 계획과 서버 상세 로그를 단조 시계로 전환했다.
- 오픈 직전 재동기화 성공 시 재앵커하고 실패 시 기존 기준을 유지한다.
- HTTP HEAD RTT를 monotonic 시간으로 측정한다.

## 검증

- 단위·fixture 테스트 116개 통과
- 타입 검사, `dist` 검증, 모듈 독립성 검증 통과
- `git diff --check` 통과

## 사용자 확인

- 확장 새로고침 후 동일 조건 반복 측정
- 로컬 로그와 서버 상세 로그의 기준 분리 확인
- 목표 클릭 계획 오차와 슬롯 감지 지연 비교
