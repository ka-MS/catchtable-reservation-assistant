# HANDOFF

**갱신:** 2026-07-11  
**브랜치:** `rebuild/open-run-mvp`  
**작업 로그:** `docs/worklog/2026-07-11-02-quantity-menu.md`

## 현재 상태

돗가비누각 실측으로 수량형 필수 메뉴 dialog를 지원했다. checkbox 없이 `<메뉴명> 수량` 입력과 추가/감소 버튼으로 구성되며, 진행 버튼이 항상 활성이므로 총수량이 예약 인원수에 도달했을 때만 진행을 클릭한다. 키워드 일치(비어 있으면 첫 메뉴) 메뉴의 수량을 1회씩 올리고, 다른 메뉴 수량은 먼저 0으로 내린다. 전환 중 일괄 비활성 재시도와 `예약금 안내` `확인` 진행은 직전 작업(2026-07-11-01) 그대로다.

## 다음 작업

1. `chrome://extensions`에서 확장 카드를 새로고침한다.
2. 돗가비누각처럼 수량형 메뉴가 있는 식당에서 인원수만큼 채워지고 진행되는지 실제 실행으로 검증한다.
3. 메뉴 단가가 예약금인 식당은 수량만큼 보증금이 커지므로 실행 전 금액을 확인한다.

## 검증

```bash
npm test
git status --short --branch
```

실행 기록은 밀리초까지 표시하며 시간 슬롯 클릭 성공 시 서버 보정 시각과 오픈 대비 지연을 함께 표시한다. 후속 단계는 메뉴 `다음/확인`, 추가 상품 무선택 진행, 예약금 안내 `확인`, 전환 중 일괄 비활성 대기, 최신 렌더 dialog 우선 판정을 지원한다. 현재 전체 코드 기준 `npm run check`: 62 tests pass + dist/independence pass.
