# HANDOFF

**갱신:** 2026-07-10  
**브랜치:** `rebuild/open-run-mvp`  
**작업 로그:** `docs/worklog/2026-07-10-01-rebuild.md`

## 현재 상태

재구축 전 상태가 `cb15c27`에 보존됐고 기존 자산 분류, 실측 이관, 새 공식 기준 확정, 과거 문서 폐기까지 완료했다. 아직 새 구현으로 교체하지 않았다.

## 다음 작업

1. 실패 구현과 테스트를 새 프로젝트 기반으로 교체한다.
2. 설정·시간·상태 머신 단위 테스트부터 TDD로 구현한다.
3. 실측 fixture를 바탕으로 Site Adapter와 오픈런 오케스트레이터를 구현한다.

## 검증

```bash
npm test
git status --short --branch
```

현재 스냅샷 기준 `npm test`: 7 pass. 이 테스트들은 새 구현 게이트가 아니며 교체 대상이다.
